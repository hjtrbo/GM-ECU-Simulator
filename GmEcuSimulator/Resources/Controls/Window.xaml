<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/Resources/Theme/Brushes.xaml"/>
        <ResourceDictionary Source="/Resources/Theme/Typography.xaml"/>
    </ResourceDictionary.MergedDictionaries>

    <!--
        Window style: WindowChrome-driven custom title bar. The window content
        sits below a 36-px caption row; the OS native min/max/close icons are
        replaced by templated buttons inside that row. ResizeBorder of 6 keeps
        edges grabbable on the smaller chrome.
    -->

    <Style x:Key="Window.AppShell" TargetType="Window">
        <Setter Property="Background" Value="{StaticResource Bg.BaseBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource Text.PrimaryBrush}"/>
        <Setter Property="FontFamily" Value="{StaticResource Font.Sans}"/>
        <Setter Property="FontSize" Value="{StaticResource Font.Size.Body}"/>
        <Setter Property="TextOptions.TextFormattingMode" Value="Ideal"/>
        <Setter Property="TextOptions.TextRenderingMode" Value="ClearType"/>
        <Setter Property="UseLayoutRounding" Value="True"/>
        <Setter Property="BorderBrush" Value="{StaticResource Border.SubtleBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="WindowChrome.WindowChrome">
            <Setter.Value>
                <shell:WindowChrome CaptionHeight="36"
                                    ResizeBorderThickness="6"
                                    CornerRadius="0"
                                    GlassFrameThickness="0"
                                    UseAeroCaptionButtons="False"/>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Dialog window (smaller modals). Same chrome, slightly thinner. -->
    <Style x:Key="Window.Dialog" TargetType="Window" BasedOn="{StaticResource Window.AppShell}">
        <Setter Property="WindowChrome.WindowChrome">
            <Setter.Value>
                <shell:WindowChrome CaptionHeight="32"
                                    ResizeBorderThickness="6"
                                    CornerRadius="0"
                                    GlassFrameThickness="0"
                                    UseAeroCaptionButtons="False"/>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>

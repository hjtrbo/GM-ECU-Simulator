<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/Resources/Theme/Brushes.xaml"/>
        <ResourceDictionary Source="/Resources/Theme/Typography.xaml"/>
    </ResourceDictionary.MergedDictionaries>

    <!-- Menu bar: dark, flat, no separator strip. Sits in the title-bar area
         on Win11-style apps, but we keep it as its own row so the chrome
         stays uncluttered. -->
    <Style TargetType="Menu">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Foreground" Value="{StaticResource Text.PrimaryBrush}"/>
        <Setter Property="FontFamily" Value="{StaticResource Font.Sans}"/>
        <Setter Property="FontSize" Value="{StaticResource Font.Size.Body}"/>
        <Setter Property="Padding" Value="0"/>
    </Style>

    <!-- Top-level MenuItem (File, J2534). Renders as a flat hot-zone with a
         hover background. We treat this as the entry point; ItemsPresenter
         hosts the descendant submenu via a templated Popup below. -->
    <ControlTemplate x:Key="MenuItem.TopLevelHeader" TargetType="MenuItem">
        <Border x:Name="Bd"
                Background="{TemplateBinding Background}"
                Padding="10,4"
                SnapsToDevicePixels="True">
            <Grid>
                <ContentPresenter ContentSource="Header"
                                  RecognizesAccessKey="True"
                                  VerticalAlignment="Center"
                                  TextElement.Foreground="{TemplateBinding Foreground}"/>
                <Popup x:Name="PART_Popup"
                       Placement="Bottom"
                       IsOpen="{TemplateBinding IsSubmenuOpen}"
                       AllowsTransparency="True"
                       Focusable="False"
                       HorizontalOffset="0"
                       VerticalOffset="2"
                       PopupAnimation="None">
                    <Border Background="{StaticResource Bg.ElevatedBrush}"
                            BorderBrush="{StaticResource Border.StrongBrush}"
                            BorderThickness="1"
                            CornerRadius="3"
                            Padding="2"
                            SnapsToDevicePixels="True">
                        <ScrollViewer CanContentScroll="True">
                            <ItemsPresenter Margin="0"
                                            KeyboardNavigation.DirectionalNavigation="Cycle"
                                            Grid.IsSharedSizeScope="True"/>
                        </ScrollViewer>
                    </Border>
                </Popup>
            </Grid>
        </Border>
        <ControlTemplate.Triggers>
            <Trigger Property="IsHighlighted" Value="True">
                <Setter TargetName="Bd" Property="Background" Value="{StaticResource Bg.HoverBrush}"/>
            </Trigger>
            <Trigger Property="IsSubmenuOpen" Value="True">
                <Setter TargetName="Bd" Property="Background" Value="{StaticResource Bg.ActiveBrush}"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Foreground" Value="{StaticResource Text.DisabledBrush}"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <!-- Submenu items (File > Open, etc). Highlight on hover, accent bar on
         selection. Includes columns for icon, header, input-gesture text, and
         submenu arrow. -->
    <ControlTemplate x:Key="MenuItem.SubmenuItem" TargetType="MenuItem">
        <Border x:Name="Bd"
                Background="{TemplateBinding Background}"
                Padding="6,4"
                CornerRadius="2"
                SnapsToDevicePixels="True">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="22"/>
                    <ColumnDefinition Width="*" SharedSizeGroup="MenuItemHeaderColumn" MinWidth="120"/>
                    <ColumnDefinition Width="Auto" SharedSizeGroup="MenuItemIGTColumn"/>
                </Grid.ColumnDefinitions>
                <ContentPresenter x:Name="Icon"
                                  ContentSource="Icon"
                                  HorizontalAlignment="Center"
                                  VerticalAlignment="Center"/>
                <ContentPresenter Grid.Column="1"
                                  ContentSource="Header"
                                  RecognizesAccessKey="True"
                                  VerticalAlignment="Center"
                                  Margin="2,0,16,0"
                                  TextElement.Foreground="{TemplateBinding Foreground}"/>
                <TextBlock Grid.Column="2"
                           Text="{TemplateBinding InputGestureText}"
                           Foreground="{StaticResource Text.TertiaryBrush}"
                           FontSize="{StaticResource Font.Size.Caption}"
                           VerticalAlignment="Center"
                           Margin="16,0,4,0"/>
            </Grid>
        </Border>
        <ControlTemplate.Triggers>
            <Trigger Property="IsHighlighted" Value="True">
                <Setter TargetName="Bd" Property="Background" Value="{StaticResource Accent.SubtleBrush}"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Foreground" Value="{StaticResource Text.DisabledBrush}"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <Style TargetType="MenuItem">
        <Setter Property="FontFamily" Value="{StaticResource Font.Sans}"/>
        <Setter Property="FontSize" Value="{StaticResource Font.Size.Body}"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{StaticResource Text.PrimaryBrush}"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Style.Triggers>
            <Trigger Property="Role" Value="TopLevelHeader">
                <Setter Property="Template" Value="{StaticResource MenuItem.TopLevelHeader}"/>
            </Trigger>
            <Trigger Property="Role" Value="TopLevelItem">
                <Setter Property="Template" Value="{StaticResource MenuItem.TopLevelHeader}"/>
            </Trigger>
            <Trigger Property="Role" Value="SubmenuHeader">
                <Setter Property="Template" Value="{StaticResource MenuItem.SubmenuItem}"/>
            </Trigger>
            <Trigger Property="Role" Value="SubmenuItem">
                <Setter Property="Template" Value="{StaticResource MenuItem.SubmenuItem}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- ContextMenu uses the same elevated-surface treatment as submenus. -->
    <Style TargetType="ContextMenu">
        <Setter Property="Background" Value="{StaticResource Bg.ElevatedBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Border.StrongBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="2"/>
        <Setter Property="Foreground" Value="{StaticResource Text.PrimaryBrush}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ContextMenu">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="3"
                            Padding="{TemplateBinding Padding}"
                            SnapsToDevicePixels="True">
                        <ItemsPresenter Grid.IsSharedSizeScope="True"
                                        KeyboardNavigation.DirectionalNavigation="Cycle"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>

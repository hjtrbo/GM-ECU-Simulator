<Window x:Class="GmEcuSimulator.Views.DbcImportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Import DBC"
        Height="620" Width="560"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        FontSize="14"
        Style="{StaticResource Window.Dialog}">

    <DockPanel LastChildFill="True">

        <!-- Custom titlebar -->
        <Grid DockPanel.Dock="Top" Background="{DynamicResource Chrome.GradientBrush}" Height="32">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Import DBC"
                       Style="{StaticResource Text.Body}"
                       FontWeight="SemiBold"
                       VerticalAlignment="Center"
                       Margin="14,0,0,0"/>
            <Button Grid.Column="1"
                    Style="{StaticResource Button.ChromeClose}"
                    Width="46" Height="32"
                    Click="OnCancelClicked"
                    ToolTip="Cancel">
                <Path Data="{StaticResource Icon.Close}"
                      Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                      StrokeThickness="1.2"
                      Width="10" Height="10"/>
            </Button>
        </Grid>

        <!-- Footer: Import replaces this ECU's broadcast set with the picked messages. -->
        <Border DockPanel.Dock="Bottom"
                Background="{DynamicResource Chrome.BgBrush}"
                BorderBrush="{DynamicResource Border.SubtleBrush}"
                BorderThickness="0,1,0,0"
                Padding="14,10">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Cancel" IsCancel="True" MinWidth="90" Click="OnCancelClicked"/>
                <Button Content="OK"
                        IsDefault="True" MinWidth="110" Margin="10,0,0,0"
                        Click="OnImportClicked"/>
            </StackPanel>
        </Border>

        <!-- Transmitter picker + select all/none -->
        <Border DockPanel.Dock="Top" Padding="14,12,14,8">
            <StackPanel>
                <TextBlock Text="{Binding FileName}" Style="{StaticResource Text.Subtitle}"/>
                <TextBlock Text="A DBC describes the whole bus. Pick the sending module, then choose which of its messages to broadcast."
                           Style="{StaticResource Text.Caption}" TextWrapping="Wrap" Margin="0,4,0,8"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Transmitter" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <ComboBox Grid.Column="1"
                              ItemsSource="{Binding Transmitters}"
                              DisplayMemberPath="Display"
                              SelectedItem="{Binding SelectedTransmitter}"
                              Height="34"/>
                    <Button Grid.Column="2" Content="Select all" Margin="8,0,0,0" Padding="10,4"
                            Command="{Binding SelectAllCommand}"/>
                    <Button Grid.Column="3" Content="None" Margin="6,0,0,0" Padding="10,4"
                            Command="{Binding SelectNoneCommand}"/>
                </Grid>
            </StackPanel>
        </Border>

        <!-- Message checklist -->
        <Border Margin="14,0,14,12" CornerRadius="4"
                BorderBrush="{DynamicResource Border.SubtleBrush}" BorderThickness="1">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Messages}" Margin="8">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <CheckBox IsChecked="{Binding Selected}" Margin="0,3">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding Display}" FontFamily="{StaticResource Font.Mono}"/>
                                    <TextBlock Text="{Binding Hint}"
                                               Foreground="{DynamicResource Accent.PrimaryBrush}"
                                               Margin="10,0,0,0" VerticalAlignment="Center"/>
                                </StackPanel>
                            </CheckBox>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>
    </DockPanel>
</Window>
